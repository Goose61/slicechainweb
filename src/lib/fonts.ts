/**
 * Font CSS variables for the app.
 *
 * Avoid next/font/google at build time — GitHub Actions / sandboxed builds
 * often fail fetching fonts.gstatic.com. Load via <link> in layout instead.
 */
export const fontClassNames = "font-sans";
